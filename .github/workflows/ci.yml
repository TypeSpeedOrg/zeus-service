name: CI

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: TypeSpeedOrg/ci-template/.github/workflows/java.yml@main
    with:
      image-name: 'zeus-backend'
      dockerfile-path: zeus-service